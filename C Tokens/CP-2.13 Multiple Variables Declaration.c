#include <stdio.h>

int main(){
  int length = 12, width = 8, height = 5;
  int volume = length*width*height;

  printf("Length: %d cm\n", length);
  printf("Width: %d cm\n", width);
  printf("Height: %d cm\n", height);
  printf("Volume: %d cubic cm\n", volume);

  return 0;
}
